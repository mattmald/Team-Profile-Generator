const Employee = require("../lib/Employee");

discribe( 'Employee', () => {
    describe("Initialization", () => {
        it("shouls see if it can create a new employee", () => {
            const emp = new Employee();
            
            expect(typeof(emp)).toBe("object");
        });

        it()





    })
});